<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        API 29+: prevent Android from auto-adding a dark scrim/gradient
        behind the navigation bar and status bar.
        Without this, some Samsung ROMs draw a semi-transparent overlay
        that makes the bars look grey/dimmed instead of our solid #07070F.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">#07070F</item>
        <item name="android:navigationBarColor">#07070F</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>

</resources>
